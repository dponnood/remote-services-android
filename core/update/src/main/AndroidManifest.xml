<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required only for the user-confirmed PackageInstaller flow on Android 8+. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <receiver
            android:name="xin.dponnood.remoteservice.core.update.InstallResultReceiver"
            android:exported="false" />
    </application>
</manifest>
