# Copy to ../keystore.properties (ignored by Git) or use the equivalent
# REMOTE_SERVICES_* environment variables. Never commit real values.
storeFile=C:/secure/remote-services-release.jks
storePassword=REPLACE_ME
keyAlias=remote-services
keyPassword=REPLACE_ME
